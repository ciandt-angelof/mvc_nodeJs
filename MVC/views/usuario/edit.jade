extends ../layout

block content
    h1 Atualizar Usuário
    hr
    br
    //- if messages.erro
    //-     div(class="alert-danger")
    //-         p #{messages.erro}
    //-     br
    form(method="POST",action="/usuario/edit/#{dados.id}",role="form")
        div(class="form-group")
            label Nome
            input(type="text", class="form-control",name="nome", palceholder="Nome",value="#{dados.nome || ''}", required, autofocus)
        div(class="form-group")
            label Site
            input(type="text", class="form-control",name="site", value="#{dados.site || ''}", palceholder="Site", required)
        div(class="form-group")
             label Email
             input(type="email", class="form-control",name="email", value="#{dados.email || 'email nao veio'}", palceholder="E-mail", readonly)
            br
        button(type="submit", class="btn btn-success") Salvar